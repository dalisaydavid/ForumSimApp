# Authenticator.py deals with user identification and whether the user is an admin or regular forum member

# return "admin" or "member" if successfully authenticated
# return False is unsuccessfully authenticated
def authenticate(userName):
	# if userName is in our database:
		return "admin" #or "admin"
	# else:
		#return False

def isAdmin(userName):
	# TODO if userName has admin rights:
	return True

